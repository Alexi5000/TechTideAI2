# Sample Inputs

Example agent inputs for testing and development.

Place JSON files here with sample prompts and inputs for different agents.
These can be used with the `run-agent` CLI tool:

```bash
npx tsx scripts/run-agent.ts --agent ceo --prompt "$(cat data/sample-inputs/ceo-strategy.txt)"
```
